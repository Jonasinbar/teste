<!-- templates/hotd.html -->
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>HotD Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900 min-h-screen flex flex-col items-center p-4">
  <div class="w-full max-w-6xl">
    <h1 class="text-3xl font-bold mb-6 text-center">Bonjour {{ username }}</h1>

    <div class="overflow-x-auto bg-white shadow-md rounded-lg">
      <table id="data-table" class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-200 sticky top-0 z-10">
          <tr id="table-headers"></tr>
        </thead>
        <tbody id="table-body" class="divide-y divide-gray-100"></tbody>
      </table>
    </div>
  </div>

  <script>
    function handleRowClick(rowData) {
      alert(`Vous avez cliqué sur : ${rowData["Nom"]}`);
    }

    fetch('/api/data')
      .then(res => res.json())
      .then(data => {
        if (data.length === 0) return;

        const orderedHeaders = ["Nom", "Age", "Ville"]; // 👈 Change column order here

        const headerRow = document.getElementById('table-headers');
        orderedHeaders.forEach(h => {
          const th = document.createElement('th');
          th.textContent = h;
          th.className = "px-6 py-3 text-left text-sm font-semibold text-gray-700";
          headerRow.appendChild(th);
        });

        const body = document.getElementById('table-body');
        data.forEach(row => {
          const tr = document.createElement('tr');
          tr.className = "hover:bg-gray-100 cursor-pointer transition-colors";
          tr.onclick = () => handleRowClick(row);

          orderedHeaders.forEach(h => {
            const td = document.createElement('td');
            td.textContent = row[h];
            td.className = "px-6 py-4 text-sm text-gray-800 whitespace-nowrap";
            tr.appendChild(td);
          });

          body.appendChild(tr);
        });
      });
  </script>
</body>
</html>
